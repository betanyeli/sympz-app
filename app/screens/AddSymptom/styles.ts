import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
    },
    title: {
        fontSize: 20,
        color: '#D38F74',
        textAlign: 'right',
        fontWeight: 'bold',
    },
})

export default styles
