import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    subContainer: {
        paddingTop: 16,
        marginHorizontal: 24,
    },
    textStyle: {
        paddingBottom: 8,
        fontSize: 16,
    },
})

export default styles
